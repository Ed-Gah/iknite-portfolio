import { Layout, UserPersonaComponent } from "@/components";
import React from "react";

const TeamPage = () => {
  return (
    <Layout>
      <UserPersonaComponent />
    </Layout>
  );
};

export default TeamPage;
